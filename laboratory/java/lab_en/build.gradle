// build.gradle — RMT-LLM Laboratory (Java, EN)
// Usage: cd java/lab_en && gradle run  OR  javac *.java && java Main

apply plugin: 'java'
apply plugin: 'application'

group = 'com.rmt.llm.lab'
version = '1.0.0'

sourceCompatibility = '17'
targetCompatibility = '17'

mainClassName = 'com.rmt.llm.lab.en.Main'

sourceSets {
    main {
        java {
            srcDirs = ['.']
            include '*.java'
        }
    }
}

repositories {
    mavenCentral()
}

// No external dependencies — pure Java SE implementation
dependencies {
}

jar {
    manifest {
        attributes 'Main-Class': 'com.rmt.llm.lab.en.Main'
    }
}

// Convenience tasks
task runEn(type: JavaExec) {
    mainClass = 'com.rmt.llm.lab.en.Main'
    classpath = sourceSets.main.runtimeClasspath
    standardInput = System.in
}
