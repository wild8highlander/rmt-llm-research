# =============================================================================
# Pre-commit hooks — run automatically on every `git commit`.
# Docs: https://pre-commit.com/
# Install:  pip install pre-commit && pre-commit install
# Manual:   pre-commit run --all-files
# Update:   pre-commit autoupdate
# =============================================================================
default_language_version:
  python: python3.12

default_install_hook_types: [pre-commit, pre-push, commit-msg]

repos:
  # ─── Pre-commit meta hooks ────────────────────────────────────────────────
  - repo: meta
    hooks:
      - id: check-hooks-apply
      - id: check-useless-excludes

  # ─── Pre-commit-built-in hooks (file hygiene) ─────────────────────────────
  - repo: https://github.com/pre-commit/pre-commit-hooks
    rev: v4.6.0
    hooks:
      - id: trailing-whitespace
        name: Trim trailing whitespace
        exclude: '\.md$'  # Markdown mandates trailing 2 spaces for hard breaks
      - id: end-of-file-fixer
        name: Ensure newline at end of file
        exclude: '\.npz$|\.parquet$|\.sqlite$|\.pdf$|\.docx$'
      - id: mixed-line-ending
        name: Normalize line endings to LF
        args: [--fix=lf]
      - id: check-merge-conflict
        name: Check for unresolved merge conflicts
      - id: check-added-large-files
        name: Block files > 500 KB
        args: [--maxkb=500]
        exclude: |
          (?x)^(
            laboratory/python/lab_en/results/models/tiny_gpt_trained\.npz|
            laboratory/python/lab_ru/results/models/tiny_gpt_trained\.npz|
            laboratory/results/models/tiny_gpt_local\.npz|
            papers/.*\.pdf|
            docs/.*\.docx|
            docs/site/demo/.*
          )$
      - id: check-case-conflict
        name: Check for case-sensitive filename conflicts
      - id: check-toml
        name: Validate TOML syntax
      - id: check-yaml
        name: Validate YAML syntax
        args: [--allow-multiple-documents]
        exclude: '^docs/site/.*$|(^|/)results/.*\.(yaml|yml|json|xml)$|^mkdocs\.yml$'
      - id: check-json
        name: Validate JSON syntax
        exclude: '\.ipynb$|/charts_3d/.*\.html$|(^|/)results/.*\.(yaml|yml|json|xml)$'
      - id: check-ast
        name: Verify Python syntax
        files: '\.py$'
      - id: debug-statements
        name: Block pdb/breakpoint() in commits
      - id: detect-private-key
        name: Block private keys (RSA, SSH, PGP)
      - id: fix-byte-order-marker
        name: Strip UTF-8 BOM
      - id: name-tests-test
        name: Test files must match test_*.py
        args: [--pytest-test-first]
        files: 'tests/.*\.py$'
        exclude: 'tests/v3/(grad_check|grad_check_precise|full_train_v3|smoke_train)\.py$'
      - id: requirements-txt-fixer
        name: Sort requirements.txt
        files: 'requirements.*\.txt$'
      - id: sort-simple-yaml
        name: Sort simple YAML files
        files: '^\.github/labels\.yml$'

  # ─── Ruff — Python lint + format (replaces black, isort, flake8) ──────────
  - repo: https://github.com/astral-sh/ruff-pre-commit
    rev: v0.16.8
    hooks:
      - id: ruff
        name: Ruff lint
        args: [--fix, --exit-non-zero-on-fix]
        files: ^(src|laboratory/python|python|rmt_llm_viz|scripts)/.*\.py$
      - id: ruff-format
        name: Ruff format
        files: ^(src|laboratory/python|python|rmt_llm_viz|scripts)/.*\.py$

  # ─── Mypy — Python type checking (advisory) ───────────────────────────────
  # ─── Vulture — find dead code ─────────────────────────────────────────────
  - repo: https://github.com/jendrikseipp/vulture
    rev: v2.11
    hooks:
      - id: vulture
        name: Vulture (dead code)
        files: ^(src|laboratory/python|scripts)/.*\.py$
        args: [--min-confidence=80]

  # ─── Interrogate — docstring coverage ─────────────────────────────────────
  - repo: https://github.com/econchick/interrogate
    rev: 1.7.0
    hooks:
      - id: interrogate
        name: Interrogate (docstring coverage)
        files: ^src/rmt_llm/.*\.py$
        args: [--config=pyproject.toml, --fail-under=70, --quiet]

  # ─── pyupgrade — upgrade Python syntax ────────────────────────────────────
  - repo: https://github.com/asottile/pyupgrade
    rev: v3.17.0
    hooks:
      - id: pyupgrade
        name: Pyupgrade
        args: [--py310-plus]
        files: ^(src|laboratory/python|scripts)/.*\.py$

  # ─── Bandit — security linter ─────────────────────────────────────────────
  - repo: https://github.com/PyCQA/bandit
    rev: 1.7.9
    hooks:
      - id: bandit
        name: Bandit (security)
        files: ^src/rmt_llm/.*\.py$
        args: [-ll, -q, --skip=B101]

  # ─── nbstripout — strip Jupyter notebook outputs before commit ────────────
  - repo: https://github.com/kynan/nbstripout
    rev: 0.7.1
    hooks:
      - id: nbstripout
        name: Nbstripout (strip notebook outputs)
        files: \.ipynb$

  # ─── check-jsonschema — validate GitHub Actions / workflow schemas ────────
  - repo: https://github.com/python-jsonschema/check-jsonschema
    rev: 0.29.2
    hooks:
      - id: check-github-workflows
        name: Validate GitHub workflows
        args: [--verbose]

  # ─── Markdown lint ────────────────────────────────────────────────────────
  - repo: https://github.com/DavidAnson/markdownlint-cli2
    rev: v0.23.2
    hooks:
      - id: markdownlint-cli2
        name: Markdown lint
        files: '\.md$'
        exclude: |
          (?x)^(
            CHANGELOG\.md|
            laboratory/webapp/.*|
            docs/site/.*
          )$

  # ─── YAML pretty-printer ──────────────────────────────────────────────────

  # ─── Toml format ──────────────────────────────────────────────────────────
  - repo: https://github.com/pappasam/toml-sort
    rev: v0.23.1
    hooks:
      - id: toml-sort
        name: TOML format
        args: [--in-place, --all]
        files: '\.toml$'

  # ─── Julia format (Formatter.jl) ──────────────────────────────────────────
  # ─── Rust format + lint ───────────────────────────────────────────────────
  # ─── Rust — cargo fmt (auto-fix) + clippy (advisory) ─────────────────────
  # cargo fmt/clippy operate per-crate; run them via `cd <crate>` local hooks
  # (newer cargo versions reject `cargo fmt -- <file>` invocations, and
  # duplicated upstream hook ids do not work reliably in pre-commit).
  - repo: local
    hooks:
      - id: rust-fmt
        name: Rust fmt (lab_en, lab_ru)
        entry: bash -c 'set -e; for d in laboratory/rust/lab_en laboratory/rust/lab_ru; do (cd "$d" && cargo fmt); done'
        language: system
        files: ^laboratory/rust/.*\.rs$
        pass_filenames: false
      - id: rust-clippy
        name: Rust clippy (lab_en, lab_ru)
        entry: bash -c 'set -e; for d in laboratory/rust/lab_en laboratory/rust/lab_ru; do (cd "$d" && cargo clippy --quiet); done'
        language: system
        files: ^laboratory/rust/.*\.rs$
        pass_filenames: false
        args: [--, -D, warnings]

  # ─── Go format + lint ─────────────────────────────────────────────────────
  - repo: https://github.com/dnephin/pre-commit-golang
    rev: v0.5.1
    hooks:
      - id: go-fmt
        name: Go fmt
        files: '\.go$'
      - id: go-mod-tidy
        name: Go mod tidy

  # ─── Dockerfile lint ──────────────────────────────────────────────────────
  - repo: https://github.com/hadolint/hadolint
    rev: v2.12.0
    hooks:
      - id: hadolint-docker
        name: Dockerfile lint (hadolint)
        files: '(^|/)Dockerfile([.].*)?$'

  # ─── Security: pip-audit ──────────────────────────────────────────────────
  - repo: https://github.com/pypa/pip-audit
    rev: v2.7.3
    hooks:
      - id: pip-audit
        name: Pip-audit (security)
        files: ^(pyproject\.toml|requirements.*\.txt)$
        args: [--requirement, requirements.txt, --strict]

  # ─── Security: gitleaks (secret scanner) ──────────────────────────────────
  - repo: https://github.com/gitleaks/gitleaks
    rev: v8.19.0
    hooks:
      - id: gitleaks
        name: Gitleaks (secret scan)

  # ─── spelling ─────────────────────────────────────────────────────────────
  - repo: https://github.com/codespell-project/codespell
    rev: v2.3.0
    hooks:
      - id: codespell
        name: Codespell
        additional_dependencies: [tomli]
        args: [--ignore-words=.codespellignore, --check-filenames, --check-hidden]
        exclude: |
          (?x)^(
            \.git/.*|
            .*\.lock|
            .*\.csv|
            .*\.npz|
            .*\.parquet|
            .*\.sqlite|
            .*\.pdf|
            .*\.docx|
            laboratory/results/.*|
            results/.*|
            CHANGELOG\.md
          )$

# ─── CI configuration ───────────────────────────────────────────────────────
ci:
  autofix_commit_msg: |
    chore(pre-commit): auto-fix from pre-commit hooks
  autofix_prs: true
  autoupdate_commit_msg: |
    chore(deps): pre-commit autoupdate
  autoupdate_schedule: weekly
  autoupdate_branch: main
  skip: [mypy, julia-formatter, go-mod-tidy, hadolint-docker, pip-audit, validate-cff, vulture, interrogate, bandit, nbstripout, check-readthedocs]
  submodules: false
